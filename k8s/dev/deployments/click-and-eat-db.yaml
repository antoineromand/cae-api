apiVersion: apps/v1
kind: Deployment
metadata:
  name: click-and-eat-db
  labels:
    app: click-and-eat-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: click-and-eat-db
  template:
    metadata:
      labels:
        app: click-and-eat-db
    spec:
      containers:
        - name: postgres
          image: postgres
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: db-secret
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-pvc
